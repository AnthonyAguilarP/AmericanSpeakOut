<ul>
    <slot />
</ul>

<script>
    import { SetupItemSelection } from "../../../utilities/SetupItemSelection";

    const masterOrder = ["nine", "eight", "seven", "six", "five", "four", "three", "two", "one", "zero"];
    // Usamos const porque el array siempre será el mismo objeto (aunque cambie su contenido)
    const currentSelection = []; 

    const items = document.querySelectorAll('.selectable');

    // Pasamos tanto el array como el orden maestro
    SetupItemSelection(items, currentSelection, masterOrder);
</script>

<style>
    ul {
        box-sizing: border-box;
        display: flex;
        flex-wrap: wrap; 
        align-items: center;
        justify-content: center;
        list-style-type: none;
        padding: 0;
        margin: 0;
        width: 100%;
        background-color: bisque;
        border: 0.3vh solid #caa06d;
    }
</style>